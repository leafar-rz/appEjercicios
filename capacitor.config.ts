import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'appEjercicios',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
